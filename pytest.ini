[pytest]
pythonpath = .
testpaths = tests
markers =
    db: needs the live MQS PostgreSQL (auto-skipped when unreachable)
    ci_db: disposable PostgreSQL only; requires CI_DATABASE_TESTS=1, never silently skips when opted in
